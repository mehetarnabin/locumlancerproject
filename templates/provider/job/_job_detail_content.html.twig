<div class="job-detail-content" data-job-id="{{ job.id }}">
    {# Header Section #}
    <div class="job-detail-header">
        <div class="job-detail-logo-container">
            <img src="{{ asset('assets/icons/job.png') }}" 
                 alt="{{ job.employer }} Logo" 
                 class="job-detail-logo"
                 onerror="this.src='{{ asset('assets/icons/job.png') }}'">
        </div>
        <div class="job-detail-info">
            <h1 class="job-detail-title">{{ job.title }}</h1>
            <div class="job-detail-company">{{ job.employer }}</div>
            <div class="job-detail-location">
                <i class="fas fa-map-marker-alt"></i>
                {{ job.state }}{% if job.city %}, {{ job.city }}{% endif %}
            </div>
        </div>
    </div>

    {# Key Metrics Grid #}
    <div class="job-detail-meta">
        <div class="job-detail-meta-item">
            <div class="job-detail-meta-label">
                <i class="fas fa-clock"></i> Schedule
            </div>
            <div class="job-detail-meta-value">{{ job.schedule ? job.schedule : 'Not specified' }}</div>
        </div>
        
        <div class="job-detail-meta-item">
            <div class="job-detail-meta-label">
                <i class="fas fa-briefcase"></i> Work Type
            </div>
            <div class="job-detail-meta-value">{{ job.workType ? job.workType : 'Not specified' }}</div>
        </div>
        
        <div class="job-detail-meta-item">
            <div class="job-detail-meta-label">
                <i class="fas fa-calendar-alt"></i> Start Date
            </div>
            <div class="job-detail-meta-value">{{ job.startDate ? job.startDate|date('m/d/Y') : 'Not specified' }}</div>
        </div>
    </div>

    {# Job Description #}
    {% if job.description %}
    <div class="job-detail-section">
        <h3 class="job-detail-section-title">
            <i class="fas fa-file-alt"></i> Job Description
        </h3>
        <div class="job-detail-description">
            {{ job.description|raw }}
        </div>
    </div>
    {% endif %}

    {# Additional Details #}
    <div class="job-detail-section">
        <h3 class="job-detail-section-title">
            <i class="fas fa-info-circle"></i> Job Details
        </h3>
        <div class="job-details-grid">
            <div class="job-detail-item">
                <strong>Job ID:</strong> {{ job.jobId }}
            </div>
            <div class="job-detail-item">
                <strong>Employer:</strong> {{ job.employer }}
            </div>
            {% if job.payRate %}
            <div class="job-detail-item">
                <strong>Pay Rate:</strong> {{ job.payRate }}
            </div>
            {% endif %}
            {% if job.payRateDaily %}
            <div class="job-detail-item">
                <strong>Pay Rate (Daily):</strong> {{ job.payRateDaily }}
            </div>
            {% endif %}
            {% if job.need %}
            <div class="job-detail-item">
                <strong>Position Need:</strong> {{ job.need }}
            </div>
            {% endif %}
            {% if job.expirationDate %}
            <div class="job-detail-item">
                <strong>Expires:</strong> {{ job.expirationDate|date('m/d/Y') }}
            </div>
            {% endif %}
        </div>
    </div>

    {# Apply Button - Only show Apply button, no Timeline button #}
    <div class="block-buttons sticky-block-buttons" style="margin-top: 24px; padding-top: 16px; border-top: 1px solid #e6eef6;">
        {% if job.id|striptags not in appliedJobsIds %}
            <button class="th-btn" id="apply-button-ajax" data-job-id="{{ job.id }}" data-redirect-route="app_provider_jobs_applications" onclick="if(typeof window.handleApplyButtonClick === 'function') { window.handleApplyButtonClick(event, this); } else { event.preventDefault(); alert('Apply functionality is loading. Please try again in a moment.'); }">
                Apply
            </button>
        {% else %}
            <button class="th-btn" style="background: #6b7280; cursor: not-allowed;" disabled>
                Already Applied
            </button>
        {% endif %}
    </div>
</div>
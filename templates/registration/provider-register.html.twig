{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {#    {% for flash_error in app.flashes('verify_email_error') %} #}
    {#        <div class="alert alert-danger" role="alert">{{ flash_error }}</div> #}
    {#    {% endfor %} #}

    {#    <h1>Register</h1> #}

    {#    {{ form_errors(registrationForm) }} #}

    {#    {{ form_start(registrationForm) }} #}
    {#        {{ form_row(registrationForm.email) }} #}
    {#        {{ form_row(registrationForm.plainPassword, { #}
    {#            label: 'Password' #}
    {#        }) }} #}
    {#        {{ form_row(registrationForm.agreeTerms) }} #}

    {#        <button type="submit" class="btn">Register</button> #}
    {#    {{ form_end(registrationForm) }} #}

    <section class="section">
        <div class="lm-container ">
            <div class="row">
                <div class="col-md-6 mb-4 d-flex justify-content-center align-items-center">
                    <div class="illustration">
                        <img src="{{ asset('assets/frontend/img/group-nurses.png') }}" alt="signup">
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-container">
                        <div class="section-title-wrap">
                            <p>Welcome back! 👋</p>
                            <h2 class="section-title ">
                                Signup
                                as
                                <strong> Provider</strong>
                            </h2>
                        </div>
                        {% for flash_error in app.flashes('verify_email_error') %}
                            <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
                        {% endfor %}

                        {{ form_errors(registrationForm) }}

                        {{ form_start(registrationForm) }}

                        <div class="form-group mb-12">
                        {{ form_row(registrationForm.name, {'attr': {'class': 'form-control', 'placeholder': 'Enter your Name'}}) }}
                        </div>

                        <div class="form-group mb-12">
                        {{ form_row(registrationForm.email, {'attr': {'class': 'form-control', 'placeholder': 'eg johndoe@example.com'}}) }}
                        </div>

                        <div class="form-group mb-12">
                        {{ form_row(registrationForm.plainPassword, {
                            'label': 'Password',
                            'attr': {'class': 'form-control', 'placeholder': 'Enter Password'}
                        }) }}
                        </div>

                        <div class="mb-12 t_c">
                            <div class="d-flex">
                                {{ form_row(registrationForm.agreeTerms, {'label' : ' '}) }}
                                <label for="remember">By clicking the checkbox, you agree to our
                                    <a href="#" class="underline">
                                        Terms and Conditions
                                    </a>
                                    and
                                    <a href="#" class="underline"> Privacy Policy</a>
                                </label>
                            </div>
                        </div>

                        <button type="submit" class="th-btn th-btn-full">Signup</button>
                        {{ form_end(registrationForm) }}

                        {#                        <form method="post" action="">#}
                        {#                            <div class="form-group mb-12">#}
                        {#                                <label for="Email">Email#}
                        {#                                    <strong>*</strong>#}
                        {#                                </label>#}
                        {#                                <input class="block " placeholder="eg. Janecopper@xyz.com" type="email" name="email">#}
                        {#                            </div>#}
                        {#                            <div class="form-group mb-12">#}
                        {#                                <label for="password">Password#}
                        {#                                    <strong>*</strong>#}
                        {#                                </label>#}
                        {#                                <input placeholder="Enter your confirm password" type="password" name="password">#}
                        {#                            </div>#}
                        {#                            <div class="form-group mb-12">#}
                        {#                                <label for="password">Confirm Password#}
                        {#                                    <strong>*</strong>#}
                        {#                                </label>#}
                        {#                                <input class="block " placeholder="Enter your confirm password" type="password"#}
                        {#                                       name="password">#}
                        {#                            </div>#}
                        {#                            <div class="mb-12 t_c">#}
                        {#                                <div class="d-flex">#}
                        {#                                    <input id="remember" required="" type="checkbox" value="" checked="">#}
                        {#                                    <label for="remember">By clicking the checkbox, you agree to our#}
                        {#                                        <a href="#" class="underline">#}
                        {#                                            Terms and Conditions#}
                        {#                                        </a>#}
                        {#                                        and#}
                        {#                                        <a href="#" class="underline"> Privacy Policy</a>#}
                        {#                                    </label>#}
                        {#                                </div>#}
                        {#                            </div>#}
                        {#                            <button class="th-btn th-btn-full" type="submit">#}
                        {#                                Sign up#}
                        {#                            </button>#}
                        {#                        </form>#}
                        <div class="existing_user">
                            <p class="text-gray-600 text-sm">
                                Already have an account?
                                <a href="{{ path('app_login') }}">
                                    Login
                                </a>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
